class Solution(object):
    def titleToNumber(self, s):
        """
        :type s: str
        :rtype: int
        """
        num=0
        for i in range(len(s)):
            num=num*26+ord(s[i])-ord('A')+1
        return num
